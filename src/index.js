import { customAlphabet } from 'nanoid';
console.log(customAlphabet("123456789",6)()); //alphabet: string, defaultSize?: number

// let genderEnum = { male: "male", female: "female" }
// console.log(Object.values(genderEnum));
import bootStrap from "./app.controller.js";

bootStrap();